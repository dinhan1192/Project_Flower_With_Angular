@{
    ViewBag.Title = "Mời chờ đợi cho đến khi trở lại trang chủ";
    Layout = "~/Views/Shared/_LayoutCustomerPage.cshtml";

    //var indexUrl = @Url.Action("Index", "Products");
}

<section>
    <div class="container">
        <div class="col-md-12 text-center">
            <div class="mb-5 element-animate ">
                <h1>@ViewBag.Title</h1>
            </div>
        </div>
    </div>
</section>

<section>
    <div class="container">
        <div class="col-md-6 col-sm-push-3 text-center">
            <h2 class="mb-5">Hello @ViewBag.UserName, confirm Email before Login!</h2>
            <div>
                Please wait 5 seconds before returning to the homepage ...
            </div>
            <div id="back2Index" data-url="@Url.Action("Index", "Home")"></div>
        </div>
    </div>
</section>

*@section scripts{
        <script>
            var url = $('#back2Index').attr("data-url");
            window.setTimeout(function () {
                location.href = url;
            }, 5000);
        </script>
    }

@*<section class="site-hero site-hero-innerpage overlay" data-stellar-background-ratio="0.5" style="background-image: url(/images/big_image_1.jpg);">
        <div class="container">
            <div class="row align-items-center site-hero-inner justify-content-center">
                <div class="col-md-8 text-center">

                    <div class="mb-5 element-animate ">
                        <h1>@ViewBag.Title</h1>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="site-section bg-light">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-5 box">
                    <h2 class="mb-5">Xin chào @ViewBag.UserName , mời xác nhận Email trước khi Login !</h2>
                    <div>
                        Mời chờ đợi 5 giây trước khi trở lại trang chủ...
                    </div>
                    <div id="back2Index" data-url="@Url.Action("Index", "Home")"></div>
                </div>
            </div>
        </div>*@
